node_modules
dist
.turbo
.vercel
.env
.env.*
!.env.example
*.local

.env*.local
tsconfig.tsbuildinfo
.prerender

# Probe scripts a review agent leaves behind; never part of the app.
_*.mjs

# Files fetched while researching an article — somebody else's licensed source, not ours.
/mao.json
/md.css
/mlf.json

# The built extension, loaded unpacked or zipped for a store — never committed.
dist-extension
